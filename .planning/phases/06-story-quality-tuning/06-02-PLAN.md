---
phase: 06-story-quality-tuning
plan: 02
type: tdd
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/lib/prompts.ts
  - src/lib/__tests__/prompts.test.ts
autonomous: true
requirements:
  - STORY-05
  - STORY-06
  - STORY-07

must_haves:
  truths:
    - "System prompt contains explicit three-part narrative arc guidance (beginning, middle, ending)"
    - "System prompt instructs wonder-framing in the middle beat, no tension or peril"
    - "System prompt instructs progressively shorter sentences in the ending"
    - "System prompt names specific sleepy sensory cues (eyelids, breathing, blanket, yawning)"
    - "System prompt contains an explicit sleep invitation instruction for the final paragraph"
    - "System prompt enumerates four distinct opening styles for variation"
    - "System prompt contains global sensory grounding instruction"
    - "buildReinforcedSystemPrompt still cascades correctly from buildSystemPrompt"
  artifacts:
    - path: "src/lib/prompts.ts"
      provides: "Rewritten buildSystemPrompt with narrative arc, wind-down, variation, and sensory instructions"
      exports: ["buildSystemPrompt", "buildReinforcedSystemPrompt", "buildUserMessage", "buildValidationPrompt"]
    - path: "src/lib/__tests__/prompts.test.ts"
      provides: "Assertions for all nine prompt decisions"
      contains: "narrative arc"
  key_links:
    - from: "src/lib/prompts.ts"
      to: "src/app/api/generate/route.ts"
      via: "buildReinforcedSystemPrompt called from route handler"
      pattern: "buildReinforcedSystemPrompt"
    - from: "src/lib/prompts.ts"
      to: "src/lib/age-levels.ts"
      via: "ReadingLevelConfig.description interpolated into prompt"
      pattern: "readingLevel\\.description"
---

<objective>
Rewrite `buildSystemPrompt()` in `prompts.ts` to produce genuinely engaging, calming, varied bedtime stories by implementing decisions D-01 through D-07 and D-09.

Purpose: Transform the system prompt from generic guidance into a structured blueprint that produces stories with clear narrative arcs, calming wind-down endings, varied openings, and rich sensory language.
Output: Rewritten `buildSystemPrompt()` function body, updated `prompts.test.ts` with assertions for all prompt decisions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-story-quality-tuning/06-CONTEXT.md
@.planning/phases/06-story-quality-tuning/06-01-SUMMARY.md

<interfaces>
<!-- Current buildSystemPrompt to be rewritten (signature preserved) -->

From src/lib/prompts.ts:
```typescript
import { ReadingLevelConfig } from "@/lib/age-levels"

export function buildSystemPrompt(
  readingLevel: ReadingLevelConfig,
  targetWords: number
): string {
  const low = Math.round(targetWords * 0.93)
  const high = Math.round(targetWords * 1.07)
  // ... returns prompt string
}

// This function calls buildSystemPrompt() and appends safety text.
// It must continue to work without modification.
export function buildReinforcedSystemPrompt(
  readingLevel: ReadingLevelConfig,
  targetWords: number
): string {
  const basePrompt = buildSystemPrompt(readingLevel, targetWords)
  return `${basePrompt}

CRITICAL SAFETY REQUIREMENTS — this story must be completely safe for bedtime:
- NO conflict, peril, danger, villains, or tension of any kind
...`
}
```

From src/lib/age-levels.ts (after Plan 01):
```typescript
export function getReadingLevel(age: number): ReadingLevelConfig
// descriptions now include sensory guidance per D-08
```

Existing prompts.test.ts assertions to preserve:
- getWordCount: 750/1500/2250 for 5/10/15 min
- getMaxTokens: 1500/3000/4096 for 5/10/15 min
- buildUserMessage: child_name XML tags, theme presence, names with spaces
- buildSystemPrompt: reading level description present, word count target present, older child vocabulary, bedtime mention, read aloud instruction, child name weaving, calming instruction, no title/headers instruction
</interfaces>
</context>

<feature>
  <name>Rewrite buildSystemPrompt with narrative arc, wind-down, variation, and sensory instructions</name>
  <files>src/lib/prompts.ts, src/lib/__tests__/prompts.test.ts</files>
  <behavior>
    Decisions to implement in the prompt:

    D-01 (STORY-05): Three-part narrative arc with paragraph counts:
    - Beginning (2-3 paragraphs): Establish the world and child's character. Warm, curious, inviting.
    - Middle (3-4 paragraphs): A gentle adventure, discovery, or small challenge. Wonder, not tension.
    - Ending (2-3 paragraphs): Resolution that slows down. Each sentence shorter than the last.

    D-02 (STORY-05): Middle beat uses "wonder" framing -- no tension, fear, or peril.

    D-03 (STORY-06): Sentence length taper -- progressively shorter sentences in the final section.

    D-04 (STORY-06): Sleepy sensory cues -- heavy eyelids, slow breathing, warmth of a blanket, yawning.

    D-05 (STORY-06): Explicit sleep invitation -- final paragraph invites child to close eyes and drift off (strengthen existing instruction).

    D-06 (STORY-07): Four opening styles enumerated:
    - In medias res: Start in the middle of action
    - Wonder: Open with a question or mystery
    - Setting: Paint the world before introducing the child
    - Character voice: Begin with something the child thinks or feels

    D-07 (STORY-07): Instruction: "Never begin two stories the same way."

    D-09 (STORY-07): Global instruction: "Ground descriptions in the senses. Favour calming, comforting sensations."

    Test cases (RED phase):
    - prompt matches /beginning/i AND /middle/i AND /ending/i (D-01)
    - prompt matches /wonder/i in context of middle beat (D-02)
    - prompt matches /shorter.*sentence|sentence.*shorter|progressively.*short/i (D-03)
    - prompt matches /eyelid|breath|blanket|yawn/i (D-04)
    - prompt matches /close.*eyes|drift.*off|eyes.*heavy/i (D-05)
    - prompt matches /in medias res|middle of action/i (D-06)
    - prompt matches /question|mystery/i for wonder opening (D-06)
    - prompt matches /setting/i for setting opening (D-06)
    - prompt matches /character.*voice|child.*thinks|child.*feels/i for character opening (D-06)
    - prompt matches /never begin two stories the same way/i (D-07)
    - prompt matches /senses|sensory/i (D-09)
    - All existing buildSystemPrompt tests still pass
  </behavior>
  <implementation>
    Rewrite the return string in buildSystemPrompt(). Keep the function signature, word-count calculation, and readingLevel.description interpolation. Organize the prompt into sections (at Claude's discretion, but sections recommended by RESEARCH.md). Keep total prompt under 400 words. Ensure the prompt ends cleanly so buildReinforcedSystemPrompt's appended safety text doesn't fall under an unexpected section header.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED -- Add prompt quality test assertions to prompts.test.ts</name>
  <files>src/lib/__tests__/prompts.test.ts</files>
  <read_first>
    - src/lib/__tests__/prompts.test.ts
    - src/lib/prompts.ts
    - .planning/phases/06-story-quality-tuning/06-CONTEXT.md
  </read_first>
  <action>
    Add a new `describe('buildSystemPrompt quality tuning (Phase 6)')` block after the existing `describe('buildSystemPrompt', ...)` block. Add tests covering all nine decisions. Use a shared setup:

    ```typescript
    describe('buildSystemPrompt quality tuning (Phase 6)', () => {
      const config = getReadingLevel(5)
      const prompt = buildSystemPrompt(config, 1500)
    ```

    Add these test cases:

    1. `it('includes three-part narrative arc guidance (D-01)')`:
       - `expect(prompt).toMatch(/beginning/i)`
       - `expect(prompt).toMatch(/middle/i)`
       - `expect(prompt).toMatch(/ending/i)`

    2. `it('specifies wonder framing for middle beat (D-02)')`:
       - `expect(prompt).toMatch(/wonder/i)`

    3. `it('instructs sentence length taper in ending (D-03)')`:
       - `expect(prompt).toMatch(/shorter.*sentence|sentence.*shorter|progressively.*short/i)`

    4. `it('names sleepy sensory cues (D-04)')`:
       - `expect(prompt).toMatch(/eyelid/i)`
       - `expect(prompt).toMatch(/breath/i)`
       - `expect(prompt).toMatch(/blanket/i)`
       - `expect(prompt).toMatch(/yawn/i)`

    5. `it('includes explicit sleep invitation (D-05)')`:
       - `expect(prompt).toMatch(/close.*eyes|drift.*off|eyes.*heavy/i)`

    6. `it('enumerates four opening styles (D-06)')`:
       - `expect(prompt).toMatch(/in medias res|middle of action/i)`
       - `expect(prompt).toMatch(/question|mystery/i)`
       - `expect(prompt).toMatch(/setting/i)`
       - `expect(prompt).toMatch(/character/i)`

    7. `it('instructs never to repeat opening styles (D-07)')`:
       - `expect(prompt).toMatch(/never begin two stories the same way/i)`

    8. `it('includes global sensory grounding instruction (D-09)')`:
       - `expect(prompt).toMatch(/senses|sensory/i)`
       - `expect(prompt).toMatch(/calming|comforting/i)`

    Run `npx vitest run src/lib/__tests__/prompts.test.ts` -- the new tests MUST FAIL (RED). All 17 existing tests MUST PASS. Commit with message: `test(06-02): add failing prompt quality tests for D-01 through D-09`
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/prompts.test.ts 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - prompts.test.ts contains the string `quality tuning (Phase 6)`
    - prompts.test.ts contains `D-01` through `D-09` in test names or comments
    - prompts.test.ts contains `expect(prompt).toMatch(/eyelid/i)`
    - prompts.test.ts contains `expect(prompt).toMatch(/in medias res|middle of action/i)`
    - prompts.test.ts contains `expect(prompt).toMatch(/never begin two stories the same way/i)`
    - vitest output shows exactly 8 new FAILED tests and 17 PASSED tests
  </acceptance_criteria>
  <done>Eight new prompt quality tests exist and fail (RED). All 17 existing tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN -- Rewrite buildSystemPrompt with all quality decisions</name>
  <files>src/lib/prompts.ts</files>
  <read_first>
    - src/lib/prompts.ts
    - src/lib/__tests__/prompts.test.ts
    - src/lib/age-levels.ts
    - .planning/phases/06-story-quality-tuning/06-CONTEXT.md
  </read_first>
  <action>
    Rewrite the return string inside `buildSystemPrompt()`. Preserve:
    - The function signature: `(readingLevel: ReadingLevelConfig, targetWords: number): string`
    - The word count calculation: `low = Math.round(targetWords * 0.93)`, `high = Math.round(targetWords * 1.07)`
    - Interpolation of `${readingLevel.description}` for reading level guidance
    - Interpolation of `${low}`, `${high}`, `${targetWords}` for word count target
    - The import of `ReadingLevelConfig`

    Do NOT modify: `buildUserMessage`, `buildValidationPrompt`, `buildReinforcedSystemPrompt`, `getWordCount`, `getMaxTokens`, `DURATION_CONFIG`.

    The new prompt string must include ALL of the following concepts (exact wording at Claude's discretion):

    **Opening line:** "You are a bedtime story writer..." (keep the role framing -- existing tests check for "bedtime")

    **Reading level section:** Interpolate `${readingLevel.description}` (existing tests check for level-specific words like "simple", "richer")

    **Word count section:** Include `${targetWords}`, `${low}`, `${high}` and mention reading aloud (existing tests check for word count and "read aloud")

    **Story Structure (D-01, D-02):**
    - Three-part structure with the words "beginning", "middle", "ending"
    - Beginning: 2-3 paragraphs, establish the world and character, warm and inviting
    - Middle: 3-4 paragraphs, gentle adventure or discovery, framed around "wonder" (not tension, not fear, not peril)
    - Ending: 2-3 paragraphs, resolution that winds down toward sleep

    **Voice and Pacing (D-03, D-04, D-05):**
    - Instruction for progressively shorter sentences in the final section (must match /shorter.*sentence|sentence.*shorter|progressively.*short/i)
    - Name these sleepy sensory cues: "eyelid" (or "eyelids"), "breath" (or "breathing"), "blanket", "yawn" (or "yawning")
    - Final paragraph must invite the child to close their eyes and drift off (must match /close.*eyes|drift.*off|eyes.*heavy/i)

    **Variety (D-06, D-07):**
    - Enumerate four opening styles: (1) in medias res / middle of action, (2) wonder / question / mystery, (3) setting, (4) character voice / what the child thinks or feels
    - Include the exact instruction: "Never begin two stories the same way."

    **Sensory Language (D-09):**
    - Include instruction about grounding descriptions in the senses with calming/comforting sensations (must match /senses|sensory/i AND /calming|comforting/i)

    **Formatting rule:** Include instruction about no title, no headers, just story text (existing test checks for this)

    **Name weaving:** Include instruction about weaving the child's name naturally (existing test checks for this)

    **Prompt length target:** Keep the total prompt under 400 words. Be concise. Each decision should map to 1-2 well-crafted sentences.

    **Ending cleanly:** The prompt string must NOT end with a section header. It must end with actual instruction text so that `buildReinforcedSystemPrompt`'s appended "CRITICAL SAFETY REQUIREMENTS" block reads as a new section, not as content under the last section header.

    Run `npx vitest run src/lib/__tests__/prompts.test.ts` -- ALL tests must PASS (GREEN).
    Run `npx vitest run` -- full suite must pass (no regressions).
    Commit with message: `feat(06-02): rewrite system prompt with narrative arc, wind-down, variation, and sensory guidance`
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/prompts.test.ts src/lib/__tests__/age-levels.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - prompts.ts buildSystemPrompt return string contains the word "beginning" (case-insensitive)
    - prompts.ts buildSystemPrompt return string contains the word "middle" (case-insensitive)
    - prompts.ts buildSystemPrompt return string contains the word "ending" (case-insensitive)
    - prompts.ts buildSystemPrompt return string contains "wonder"
    - prompts.ts buildSystemPrompt return string contains "eyelid" or "eyelids"
    - prompts.ts buildSystemPrompt return string contains "blanket"
    - prompts.ts buildSystemPrompt return string contains "yawn" or "yawning"
    - prompts.ts buildSystemPrompt return string contains "in medias res" or "middle of action"
    - prompts.ts buildSystemPrompt return string contains "Never begin two stories the same way"
    - prompts.ts buildSystemPrompt return string contains "senses" or "sensory"
    - prompts.ts buildSystemPrompt return string contains "close" and "eyes" or "drift" and "off"
    - buildSystemPrompt function signature is still `(readingLevel: ReadingLevelConfig, targetWords: number): string`
    - buildReinforcedSystemPrompt, buildUserMessage, buildValidationPrompt, getWordCount, getMaxTokens are unchanged
    - vitest run (full suite) shows 0 failures
  </acceptance_criteria>
  <done>All prompt tests pass (existing + new). buildSystemPrompt implements D-01 through D-07 and D-09. Full test suite green.</done>
</task>

</tasks>

<verification>
npx vitest run src/lib/__tests__/prompts.test.ts -- all tests pass (existing 17 + new 8 = 25)
npx vitest run src/lib/__tests__/age-levels.test.ts -- all 12 tests pass
npx vitest run -- full suite passes (no regressions)
</verification>

<success_criteria>
- buildSystemPrompt contains structured narrative arc guidance (beginning/middle/ending with paragraph counts)
- Middle beat uses wonder framing, explicitly no tension/fear/peril
- Ending instructs sentence length taper and names sleepy sensory cues
- Four opening styles enumerated for story variation
- Global sensory grounding instruction present
- Prompt stays under 400 words
- buildReinforcedSystemPrompt still works (cascades from updated base prompt)
- All tests pass (existing + new)
</success_criteria>

<output>
After completion, create `.planning/phases/06-story-quality-tuning/06-02-SUMMARY.md`
</output>
