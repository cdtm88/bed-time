---
phase: 06-story-quality-tuning
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/age-levels.ts
  - src/lib/__tests__/age-levels.test.ts
autonomous: true
requirements:
  - STORY-07

must_haves:
  truths:
    - "Toddler reading level description includes tactile and sound sensory words"
    - "Young child reading level description includes gentle multi-sense descriptions"
    - "Older child reading level description includes rich multi-sense imagery vocabulary"
    - "Existing reading level assertions still pass (level mapping, description basics)"
  artifacts:
    - path: "src/lib/age-levels.ts"
      provides: "Per-age sensory guidance in ReadingLevelConfig descriptions"
      contains: "soft|warm|fluffy"
    - path: "src/lib/__tests__/age-levels.test.ts"
      provides: "Sensory guidance assertions for all three age bands"
      contains: "sensory"
  key_links:
    - from: "src/lib/age-levels.ts"
      to: "src/lib/prompts.ts"
      via: "readingLevel.description interpolated into system prompt"
      pattern: "readingLevel\\.description"
---

<objective>
Add per-age-level sensory guidance to the `description` field of each reading level in `age-levels.ts`, per decision D-08.

Purpose: Enriches the system prompt with age-appropriate sensory vocabulary so Claude produces stories with tactile, auditory, and visual imagery calibrated to the child's developmental stage.
Output: Updated `age-levels.ts` with expanded descriptions, updated `age-levels.test.ts` with sensory assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-story-quality-tuning/06-CONTEXT.md

<interfaces>
<!-- Key types the executor needs -->

From src/lib/age-levels.ts:
```typescript
export type ReadingLevel = "toddler" | "young_child" | "older_child"

export interface ReadingLevelConfig {
  level: ReadingLevel
  description: string
}

export function getReadingLevel(age: number): ReadingLevelConfig
```

Current descriptions (to be expanded):
- Toddler (age 0-3): "Very simple sentences. Familiar, everyday words. Repetition encouraged. Short paragraphs."
- Young child (age 4-6): "Short sentences with some descriptive language. Gentle complexity. Simple dialogue."
- Older child (age 7-10): "Longer sentences with richer vocabulary. More involved narrative. Descriptive imagery."
</interfaces>
</context>

<feature>
  <name>Per-age sensory guidance in reading level descriptions</name>
  <files>src/lib/age-levels.ts, src/lib/__tests__/age-levels.test.ts</files>
  <behavior>
    Per D-08 from CONTEXT.md:

    - Toddler (age 0-3): Append sensory guidance about simple tactile and sound words. Must include words like "soft", "warm", "fluffy", "splashing", "humming". Short, concrete sensory language.
    - Young child (age 4-6): Append sensory guidance about gentle multi-sense descriptions. Must reference concepts like smell of rain, feel of cool grass, glow of moonlight. Gentle and grounded.
    - Older child (age 7-10): Append sensory guidance about richer multi-sense imagery across all senses -- sight, sound, smell, touch. Vivid but calming.

    Test cases (RED phase):
    - getReadingLevel(2).description matches /soft|warm|fluffy|splash|hum/i
    - getReadingLevel(5).description matches /smell|feel|glow|rain|grass|moonlight/i
    - getReadingLevel(8).description matches /sight|sound|smell|touch|vivid/i
    - All 9 existing tests in age-levels.test.ts still pass
  </behavior>
  <implementation>
    Expand each `description` string in `getReadingLevel()` by appending a sensory guidance sentence after the existing reading-level text. Keep each description concise (1-2 sentences added). Do not change the function signature or the `level` field values.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED -- Add sensory guidance test assertions to age-levels.test.ts</name>
  <files>src/lib/__tests__/age-levels.test.ts</files>
  <read_first>
    - src/lib/__tests__/age-levels.test.ts
    - src/lib/age-levels.ts
    - .planning/phases/06-story-quality-tuning/06-CONTEXT.md
  </read_first>
  <action>
    Add a new `describe('getReadingLevel sensory guidance (D-08)')` block at the end of the existing test file (after the existing `describe('getReadingLevel', ...)` block). Add three test cases:

    1. `it('toddler includes tactile and sound sensory words', ...)`:
       ```typescript
       const result = getReadingLevel(2)
       expect(result.description).toMatch(/soft|warm|fluffy|splash|hum/i)
       ```

    2. `it('young child includes multi-sense descriptions', ...)`:
       ```typescript
       const result = getReadingLevel(5)
       expect(result.description).toMatch(/smell|feel|glow|rain|grass|moonlight/i)
       ```

    3. `it('older child includes rich multi-sense imagery', ...)`:
       ```typescript
       const result = getReadingLevel(8)
       expect(result.description).toMatch(/sight|sound|smell|touch|vivid/i)
       ```

    Run `npx vitest run src/lib/__tests__/age-levels.test.ts` -- the 3 new tests MUST FAIL (RED). The 9 existing tests MUST PASS. Commit with message: `test(06-01): add failing sensory guidance tests for age levels (D-08)`
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/age-levels.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - age-levels.test.ts contains the string `sensory guidance (D-08)`
    - age-levels.test.ts contains `expect(result.description).toMatch(/soft|warm|fluffy|splash|hum/i)`
    - age-levels.test.ts contains `expect(result.description).toMatch(/smell|feel|glow|rain|grass|moonlight/i)`
    - age-levels.test.ts contains `expect(result.description).toMatch(/sight|sound|smell|touch|vivid/i)`
    - vitest output shows exactly 3 FAILED tests and 9 PASSED tests
  </acceptance_criteria>
  <done>Three new sensory guidance tests exist and fail (RED). All 9 existing tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN -- Expand age-level descriptions with sensory guidance</name>
  <files>src/lib/age-levels.ts</files>
  <read_first>
    - src/lib/age-levels.ts
    - src/lib/__tests__/age-levels.test.ts
    - .planning/phases/06-story-quality-tuning/06-CONTEXT.md
  </read_first>
  <action>
    Expand each `description` string in `getReadingLevel()` by appending sensory guidance after the existing text. Keep each addition to 1-2 sentences. The function signature, return type, and `level` values remain unchanged.

    Toddler (age <= 3) -- append after "Short paragraphs.":
    Sensory guidance about simple tactile and sound words: soft, warm, fluffy, splashing, humming. Keep sensory language short and concrete.

    Young child (age <= 6) -- append after "Simple dialogue.":
    Sensory guidance about gentle multi-sense descriptions: the smell of rain, the feel of cool grass, the glow of moonlight. Grounded and gentle.

    Older child (age 7+) -- append after "Descriptive imagery.":
    Sensory guidance about richer multi-sense imagery across sight, sound, smell, and touch. Vivid but calming.

    The exact wording is at Claude's discretion (per CONTEXT.md), but the description strings MUST contain the words needed to pass the test assertions from Task 1.

    Run `npx vitest run src/lib/__tests__/age-levels.test.ts` -- ALL 12 tests must PASS (GREEN). Commit with message: `feat(06-01): add per-age sensory guidance to reading levels (D-08)`
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/age-levels.test.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - age-levels.ts toddler description contains at least one of: "soft", "warm", "fluffy", "splash", "hum" (case-insensitive)
    - age-levels.ts young_child description contains at least one of: "smell", "feel", "glow", "rain", "grass", "moonlight" (case-insensitive)
    - age-levels.ts older_child description contains at least one of: "sight", "sound", "smell", "touch", "vivid" (case-insensitive)
    - getReadingLevel function signature is still `(age: number): ReadingLevelConfig`
    - vitest output shows 12 tests PASSED, 0 FAILED
    - `npx vitest run` (full suite) passes with no failures
  </acceptance_criteria>
  <done>All 12 age-levels tests pass. Descriptions now include age-appropriate sensory guidance per D-08.</done>
</task>

</tasks>

<verification>
npx vitest run src/lib/__tests__/age-levels.test.ts -- 12 tests pass
npx vitest run -- full suite passes (no regressions)
</verification>

<success_criteria>
- All 3 reading levels have sensory guidance in their descriptions
- Toddler: tactile/sound words (soft, warm, fluffy, etc.)
- Young child: multi-sense gentle (smell of rain, feel of grass, glow of moonlight)
- Older child: rich multi-sense vivid but calming (sight, sound, smell, touch)
- All existing tests still pass
- Function signature unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/06-story-quality-tuning/06-01-SUMMARY.md`
</output>
