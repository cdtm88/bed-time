---
phase: 05-reading-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/components/story-form.tsx
  - vitest.config.ts
  - src/__tests__/reading-view.test.ts
autonomous: true
requirements:
  - READ-01
must_haves:
  truths:
    - "SessionStorage payload contains story, name, and theme as JSON"
    - "Dark reading mode CSS tokens are available as Tailwind utilities"
    - "Tests exist for sessionStorage parsing, empty state, error state, paragraph splitting, and scroll progress"
  artifacts:
    - path: "src/app/globals.css"
      provides: "5 dark reading mode color tokens inside @theme block"
      contains: "--color-reading-surface"
    - path: "src/components/story-form.tsx"
      provides: "JSON sessionStorage write with story, name, theme"
      contains: "JSON.stringify"
    - path: "src/__tests__/reading-view.test.ts"
      provides: "Unit tests for ReadingView behaviors"
      min_lines: 50
    - path: "vitest.config.ts"
      provides: "jsdom environment for browser API tests"
      contains: "jsdom"
  key_links:
    - from: "src/components/story-form.tsx"
      to: "sessionStorage"
      via: "JSON.stringify({ story, name, theme })"
      pattern: "JSON\\.stringify.*story.*name.*theme"
    - from: "src/app/globals.css"
      to: "Tailwind utilities"
      via: "@theme block --color-reading-* tokens"
      pattern: "--color-reading-surface"
---

<objective>
Prepare the data contract, design tokens, and test scaffolding for the reading experience.

Purpose: Establish the foundations that the ReadingView component (Plan 02) will build on -- the sessionStorage JSON contract, dark reading mode CSS tokens, and failing tests that define expected behavior.
Output: Updated story-form.tsx (JSON write), globals.css (5 dark tokens), vitest config (jsdom), and test file with failing tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-reading-experience/05-CONTEXT.md
@.planning/phases/05-reading-experience/05-UI-SPEC.md
@.planning/phases/05-reading-experience/05-RESEARCH.md
@.planning/phases/05-reading-experience/05-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 test scaffolding and Vitest jsdom environment</name>
  <files>vitest.config.ts, src/__tests__/reading-view.test.ts</files>
  <read_first>
    - vitest.config.ts (current config -- need to add jsdom environment)
    - src/lib/__tests__/rate-limit.test.ts (reference test pattern for this project)
    - .planning/phases/05-reading-experience/05-VALIDATION.md (test map)
    - .planning/phases/05-reading-experience/05-RESEARCH.md (code examples for sessionStorage parsing, paragraph splitting, scroll progress)
  </read_first>
  <behavior>
    - Test "parses story data": Given sessionStorage contains JSON.stringify({ story: "Once upon...", name: "Luna", theme: "Space" }), the parse function returns { story: "Once upon...", name: "Luna", theme: "Space" }
    - Test "empty state": Given sessionStorage has no "nightlight-story" key, the parse function returns null
    - Test "error state": Given sessionStorage contains invalid JSON "not-json{", the parse function throws or returns an error indicator
    - Test "paragraph splitting": Given story text "Para one.\n\nPara two.\n\nPara three.", splitting on "\n\n" and filtering empty produces ["Para one.", "Para two.", "Para three."]
    - Test "paragraph splitting empty lines": Given story text with extra whitespace "\n\n\n\n", filtering produces an empty array
    - Test "scroll progress": Given scrollTop=500, scrollHeight=1500, innerHeight=500, progress = 500/(1500-500) = 0.5
    - Test "scroll progress zero height": Given scrollHeight equals innerHeight (no scroll needed), progress = 0 (no division by zero)
    - Test "title assembly": Given name="Luna" and theme="Space", title = "Luna's Space Story"
  </behavior>
  <action>
    1. Install jsdom as a dev dependency: `npm install -D jsdom`

    2. Update `vitest.config.ts` to add jsdom environment for the reading-view tests. The config currently has:
    ```typescript
    import { defineConfig } from 'vitest/config'
    import path from 'path'

    export default defineConfig({
      test: {
        include: ['src/**/__tests__/**/*.test.ts'],
      },
      resolve: {
        alias: {
          '@': path.resolve(__dirname, './src'),
        },
      },
    })
    ```

    Add `environment: 'jsdom'` to the test block. This enables sessionStorage and window globals in all tests. The existing library tests (schemas, age-levels, prompts, safety, rate-limit, theme-utils) do not use browser APIs, so jsdom is a no-op for them.

    3. Create `src/__tests__/reading-view.test.ts` with tests for pure utility functions that will be extracted from the ReadingView component. Structure the test file to test these pure functions directly (not React components):

    Define and test these functions inline in the test file for now (they will be moved to the component or a utility in Plan 02):

    ```typescript
    // parseStoryData: reads sessionStorage, returns StoryData | null, throws on bad JSON
    // splitParagraphs: splits on '\n\n', filters empty strings
    // calculateScrollProgress: (scrollTop, scrollHeight, innerHeight) => number 0-1
    // assembleTitle: (name, theme) => string
    ```

    Interface for StoryData:
    ```typescript
    interface StoryData {
      story: string
      name: string
      theme: string
    }
    ```

    Function signatures to implement in the test file as stubs that will fail:
    - `parseStoryData(): StoryData | null` -- reads from sessionStorage key "nightlight-story", returns parsed object or null if missing, throws if invalid JSON
    - `splitParagraphs(text: string): string[]` -- splits on "\n\n", filters out empty/whitespace-only strings
    - `calculateScrollProgress(scrollTop: number, scrollHeight: number, innerHeight: number): number` -- returns 0-1, guards against division by zero
    - `assembleTitle(name: string, theme: string): string` -- returns "{Name}'s {Theme} Story"

    Write the stub implementations that throw "not implemented" so tests RED initially. Then implement the actual functions to make tests GREEN.

    The test file should use `describe`/`it`/`expect` from vitest. Use `beforeEach` to clear sessionStorage between tests.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/reading-view.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - vitest.config.ts contains `environment: 'jsdom'`
    - src/__tests__/reading-view.test.ts exists and contains at least 8 test cases
    - src/__tests__/reading-view.test.ts contains `describe('parseStoryData'`
    - src/__tests__/reading-view.test.ts contains `describe('splitParagraphs'`
    - src/__tests__/reading-view.test.ts contains `describe('calculateScrollProgress'`
    - src/__tests__/reading-view.test.ts contains `describe('assembleTitle'`
    - `npx vitest run src/__tests__/reading-view.test.ts` exits 0 with all tests passing
    - `npx vitest run` exits 0 (all existing tests still pass with jsdom environment)
  </acceptance_criteria>
  <done>All 8+ reading-view tests pass green. Vitest configured with jsdom. Existing test suite unbroken.</done>
</task>

<task type="auto">
  <name>Task 2: CSS dark tokens and sessionStorage JSON contract</name>
  <files>src/app/globals.css, src/components/story-form.tsx</files>
  <read_first>
    - src/app/globals.css (current @theme block -- append new tokens inside it)
    - src/components/story-form.tsx (current sessionStorage write on lines 44-45)
    - .planning/phases/05-reading-experience/05-UI-SPEC.md (CSS Token Additions section)
    - .planning/phases/05-reading-experience/05-CONTEXT.md (D-01, D-02 locked decisions)
  </read_first>
  <action>
    1. **globals.css**: Append 5 new color tokens INSIDE the existing `@theme { }` block (lines 3-21). The block currently ends with `--spacing-3xl: 64px;` on line 20 and `}` on line 21. Add these tokens before the closing brace:

    ```css
    /* Reading mode (Phase 5) */
    --color-reading-surface: #1a1a2e;
    --color-reading-surface-elevated: #232340;
    --color-reading-on-surface: #e8e0d0;
    --color-reading-on-surface-muted: #a89f8f;
    --color-on-secondary-container: #1b1c17;
    ```

    These will auto-map to Tailwind utilities: `bg-reading-surface`, `text-reading-on-surface`, `text-reading-on-surface-muted`, `bg-reading-surface-elevated`, `text-on-secondary-container`.

    2. **story-form.tsx**: Update the sessionStorage write (lines 44-45) per D-01 and D-02.

    Current code:
    ```typescript
    sessionStorage.setItem('nightlight-story', storyText)
    sessionStorage.setItem('nightlight-story-name', name.trim())
    ```

    Replace with:
    ```typescript
    sessionStorage.setItem('nightlight-story', JSON.stringify({
      story: storyText,
      name: name.trim(),
      theme: theme,
    }))
    ```

    Remove the `sessionStorage.setItem('nightlight-story-name', name.trim())` line entirely -- `name` is now inside the JSON payload per D-01.

    No other changes to story-form.tsx. The `theme` variable is already in scope as component state (`const [theme, setTheme] = useState<string | null>(null)`). By the time `handleSubmit` runs, `theme` is guaranteed non-null because `canSubmit` requires `theme !== null`.
  </action>
  <verify>
    <automated>npx vitest run && grep -c "reading-surface" src/app/globals.css && grep "JSON.stringify" src/components/story-form.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/app/globals.css contains `--color-reading-surface: #1a1a2e;` inside the @theme block
    - src/app/globals.css contains `--color-reading-surface-elevated: #232340;`
    - src/app/globals.css contains `--color-reading-on-surface: #e8e0d0;`
    - src/app/globals.css contains `--color-reading-on-surface-muted: #a89f8f;`
    - src/app/globals.css contains `--color-on-secondary-container: #1b1c17;`
    - src/components/story-form.tsx contains `JSON.stringify({`
    - src/components/story-form.tsx contains `story: storyText`
    - src/components/story-form.tsx contains `name: name.trim()`
    - src/components/story-form.tsx contains `theme: theme` or `theme,`
    - src/components/story-form.tsx does NOT contain `nightlight-story-name` (removed per D-01)
    - `npx vitest run` exits 0
  </acceptance_criteria>
  <done>5 dark reading mode tokens added to globals.css @theme block. SessionStorage write updated to JSON payload with story, name, and theme. Legacy nightlight-story-name key removed.</done>
</task>

</tasks>

<verification>
- `npx vitest run` passes all tests (existing + new reading-view tests)
- `grep "reading-surface" src/app/globals.css` shows 2 matches (surface + elevated)
- `grep "JSON.stringify" src/components/story-form.tsx` shows the JSON write
- `grep -c "nightlight-story-name" src/components/story-form.tsx` returns 0
</verification>

<success_criteria>
- All reading-view unit tests pass green
- Dark reading mode CSS tokens are registered in @theme and usable as Tailwind utilities
- SessionStorage contract updated from raw text to JSON { story, name, theme }
- All existing tests remain green
</success_criteria>

<output>
After completion, create `.planning/phases/05-reading-experience/05-01-SUMMARY.md`
</output>
