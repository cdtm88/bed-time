---
phase: 01-project-scaffolding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - postcss.config.mjs
  - eslint.config.mjs
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
  - src/app/api/generate/route.ts
  - src/components/.gitkeep
  - src/lib/.gitkeep
  - .env.local.example
  - .gitignore
autonomous: false
requirements:
  - INFRA-04

user_setup:
  - service: vercel
    why: "Deployment hosting for Next.js app"
    account: "Create free account at https://vercel.com/signup if not existing"
    dashboard_config:
      - task: "Import GitHub repo as new Vercel project"
        location: "vercel.com -> Add New Project -> Import Git Repository -> select bed-time repo"

must_haves:
  truths:
    - "Next.js app builds without errors via npm run build"
    - "Dev server starts and renders a styled page at localhost:3000"
    - "Tailwind CSS utility classes produce visible styles in the browser"
    - "Edge Runtime stub at /api/generate compiles and responds to POST requests"
    - "App is deployed on Vercel and accessible via a public URL"
    - "Pushes to main trigger automatic Vercel redeployment"
  artifacts:
    - path: "src/app/api/generate/route.ts"
      provides: "Edge Runtime stub for story generation"
      contains: "export const runtime = \"edge\""
    - path: "src/app/page.tsx"
      provides: "Placeholder home page with Tailwind styles"
      contains: "Nightlight Tales"
    - path: "src/app/globals.css"
      provides: "Tailwind v4 CSS entry point"
      contains: "@import \"tailwindcss\""
    - path: "postcss.config.mjs"
      provides: "Tailwind v4 PostCSS plugin configuration"
      contains: "@tailwindcss/postcss"
    - path: ".env.local.example"
      provides: "Environment variable template"
      contains: "ANTHROPIC_API_KEY="
  key_links:
    - from: "src/app/globals.css"
      to: "postcss.config.mjs"
      via: "PostCSS build pipeline"
      pattern: "@import \"tailwindcss\""
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "CSS import in root layout"
      pattern: "import.*globals.css"
    - from: "GitHub push to main"
      to: "Vercel deployment"
      via: "Vercel GitHub integration"
      pattern: "auto-deploy on push"
---

<objective>
Scaffold a Next.js 16 application with Tailwind CSS v4, an Edge Runtime API stub, and deploy it to Vercel via GitHub integration.

Purpose: Establish the foundation that every subsequent phase builds into -- a deployed, working app skeleton with the correct folder structure, build tooling, and deployment pipeline.
Output: A publicly accessible Vercel deployment of a Next.js app with Tailwind CSS rendering, Edge Runtime stub compiling, and automatic deploys on push to main.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-scaffolding/01-CONTEXT.md
@.planning/phases/01-project-scaffolding/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 16 project with Edge Runtime stub and placeholder structure</name>
  <files>
    package.json, tsconfig.json, next.config.ts, postcss.config.mjs, eslint.config.mjs,
    src/app/layout.tsx, src/app/page.tsx, src/app/globals.css,
    src/app/api/generate/route.ts, src/components/.gitkeep, src/lib/.gitkeep,
    .env.local.example, .gitignore
  </files>
  <read_first>
    .planning/phases/01-project-scaffolding/01-RESEARCH.md (verified patterns, anti-patterns, exact code examples)
    .planning/phases/01-project-scaffolding/01-CONTEXT.md (locked decisions D-01 through D-05)
  </read_first>
  <action>
    1. Run `npx create-next-app@latest bed-time-app --yes` from a temporary location, then move the generated files into the repo root (the repo already exists with .planning/ content, so scaffold into a temp dir and merge).

       Alternatively, since the repo root already has content, run the scaffold into a subdirectory and move files:
       ```
       cd /tmp
       npx create-next-app@latest bed-time-scaffold --yes
       ```
       Then copy the scaffolded files (excluding .git/) into /Users/christianmoore/ai/bed-time/, preserving the existing .planning/ directory.

    2. Verify the scaffold produced the expected structure (per D-04, D-05):
       - `src/app/` directory exists (App Router with src/ wrapper)
       - `postcss.config.mjs` exists with `"@tailwindcss/postcss": {}` plugin (Tailwind v4 -- NOT tailwind.config.js)
       - `src/app/globals.css` contains `@import "tailwindcss"` (Tailwind v4 -- NOT v3 @tailwind directives)
       - `tsconfig.json` has `@/*` path alias
       - `next.config.ts` exists (TypeScript config, not .js)

    3. If `create-next-app` produced a `tailwind.config.js` or `tailwind.config.ts`, DELETE it -- Tailwind v4 does not use config files (per D-01 and research anti-patterns).

    4. If `postcss.config.mjs` uses `"tailwindcss"` as the plugin name instead of `"@tailwindcss/postcss"`, replace it. The correct content is:
       ```javascript
       const config = {
         plugins: {
           "@tailwindcss/postcss": {},
         },
       };
       export default config;
       ```

    5. Create the Edge Runtime API stub (per D-03):
       - Create directory: `src/app/api/generate/`
       - Create file: `src/app/api/generate/route.ts` with this exact content:
         ```typescript
         export const runtime = "edge"

         export async function POST(_request: Request) {
           return new Response("Story generation coming in Phase 2", {
             status: 200,
             headers: { "Content-Type": "text/plain" },
           })
         }
         ```
       - CRITICAL: Use `"edge"` NOT `"experimental-edge"` (removed in Next.js 16).

    6. Replace `src/app/page.tsx` with a minimal placeholder that uses Tailwind classes to verify styling works:
       ```typescript
       export default function Home() {
         return (
           <main className="flex min-h-screen items-center justify-center">
             <h1 className="text-2xl font-bold">Nightlight Tales</h1>
           </main>
         )
       }
       ```

    7. Ensure `src/app/globals.css` contains ONLY:
       ```css
       @import "tailwindcss";
       ```
       Remove any default Next.js boilerplate styles. Do NOT add design tokens -- per D-01, that work belongs to Phase 4/5.

    8. Create placeholder directories (per D-04):
       - `src/components/.gitkeep` (empty file)
       - `src/lib/.gitkeep` (empty file)

    9. Create `.env.local.example` at the repo root with this exact content:
       ```
       # Copy this file to .env.local and fill in your values
       # Never commit .env.local to git

       ANTHROPIC_API_KEY=
       ```

    10. Verify `.gitignore` includes `*.local` pattern (create-next-app includes this by default; confirm it is present so `.env.local` is never committed).

    11. Verify the dev server scripts in `package.json`:
        - `"dev"` should be `"next dev"` (NOT `"next dev --turbo"` -- Turbopack is default in Next.js 16, --turbo flag removed)
        - `"build"` should be `"next build"`
        - `"start"` should be `"next start"`

    12. Run `npm run build` to verify everything compiles: TypeScript, Tailwind v4 PostCSS, Edge Runtime stub.

    13. Run `npm run dev` briefly and curl localhost:3000 to confirm the page renders. Also curl -X POST localhost:3000/api/generate to confirm the Edge stub returns "Story generation coming in Phase 2".
  </action>
  <verify>
    <automated>cd /Users/christianmoore/ai/bed-time && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits with code 0 (no TypeScript errors, no Tailwind errors, no Edge Runtime errors)
    - `src/app/api/generate/route.ts` contains `export const runtime = "edge"`
    - `src/app/api/generate/route.ts` contains `export async function POST`
    - `src/app/page.tsx` contains `Nightlight Tales`
    - `src/app/page.tsx` contains `className="flex min-h-screen`
    - `src/app/globals.css` contains `@import "tailwindcss"`
    - `src/app/globals.css` does NOT contain `@tailwind base` (v3 pattern)
    - `postcss.config.mjs` contains `@tailwindcss/postcss`
    - No file named `tailwind.config.js` or `tailwind.config.ts` exists in the repo root
    - `.env.local.example` contains `ANTHROPIC_API_KEY=`
    - `.gitignore` contains `*.local`
    - `src/components/.gitkeep` exists
    - `src/lib/.gitkeep` exists
    - `package.json` dev script is `"next dev"` (not `"next dev --turbo"`)
    - `curl -X POST http://localhost:3000/api/generate` returns "Story generation coming in Phase 2" (tested during task execution)
  </acceptance_criteria>
  <done>
    Next.js 16 project scaffolded with Tailwind v4, Edge Runtime stub at /api/generate, placeholder directories, and .env.local.example. Build passes cleanly. No design tokens configured (per D-01). No tailwind.config.js (Tailwind v4). No --turbo flag (Next.js 16 default).
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Connect GitHub repo to Vercel and verify deployment</name>
  <files>N/A -- Vercel dashboard configuration, no local files modified</files>
  <read_first>
    .planning/phases/01-project-scaffolding/01-RESEARCH.md (Pattern 3: Vercel GitHub Integration steps)
  </read_first>
  <action>
    Before this checkpoint: Claude commits all scaffolded files and pushes to GitHub main branch.

    Human performs these steps in the Vercel dashboard (per D-02 -- GitHub pipeline, not CLI):

    1. Go to https://vercel.com (create free account at https://vercel.com/signup if needed)
    2. Click "Add New Project"
    3. Click "Import Git Repository"
    4. Select the `bed-time` repository from your GitHub account
    5. Vercel will auto-detect Next.js -- accept the default settings (no vercel.json needed, no changes)
    6. Click "Deploy"
    7. Wait for build to complete (~60 seconds)

    Then verify:
    1. Vercel dashboard shows successful deployment (green checkmark)
    2. Navigate to the deployment URL (e.g., bed-time-xxx.vercel.app) -- page shows "Nightlight Tales" centered with bold text
    3. Run: `curl -X POST https://{your-deployment-url}/api/generate` -- returns "Story generation coming in Phase 2"
    4. Confirm Vercel project settings show "Automatic Deployments" enabled for main branch
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" -X POST https://{VERCEL_URL}/api/generate</automated>
  </verify>
  <acceptance_criteria>
    - Vercel dashboard shows successful deployment with green status
    - Public URL (*.vercel.app) returns HTTP 200 and renders "Nightlight Tales"
    - POST to {public-url}/api/generate returns HTTP 200 with body "Story generation coming in Phase 2"
    - Vercel project settings show automatic deployments enabled for main branch
  </acceptance_criteria>
  <done>
    App is deployed on Vercel via GitHub integration. Public URL is accessible. Edge Runtime stub responds. Auto-deploy on push to main is enabled. INFRA-04 is satisfied.
  </done>
</task>

</tasks>

<verification>
Phase 1 is complete when:
1. `npm run build` passes with zero errors
2. `npm run dev` serves a styled page at localhost:3000
3. The Edge Runtime stub at /api/generate responds to POST requests
4. The app is deployed on Vercel via GitHub integration
5. The public Vercel URL is accessible and renders the styled placeholder
6. Automatic deploys are enabled (push to main triggers redeploy)
</verification>

<success_criteria>
- INFRA-04 satisfied: App is deployed on Vercel with zero-config hosting and edge functions
- Phase 1 success criteria from ROADMAP met:
  1. Next.js app deployed on Vercel, accessible via public URL
  2. Development environment runs locally with hot reload via Turbopack (default in Next.js 16)
  3. Tailwind CSS configured and rendering styles correctly
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-scaffolding/01-01-SUMMARY.md`
</output>
