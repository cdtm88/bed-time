---
phase: 11-ui-polish-and-tidy-up
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/hero-illustration.tsx
  - src/components/story-form.tsx
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06

must_haves:
  truths:
    - "Form page shows a decorative moon + stars illustration above the title"
    - "Hero illustration echoes loading overlay moon/stars visual language"
    - "Generate button is solid flat teal (no gradient)"
    - "Form content is horizontally centered with balanced padding"
    - "Top padding is reduced to compensate for hero illustration height"
  artifacts:
    - path: "src/components/hero-illustration.tsx"
      provides: "Decorative moon + stars SVG illustration component"
      contains: "gentleBreathe"
    - path: "src/components/story-form.tsx"
      provides: "Updated form with hero, solid button, adjusted spacing"
      contains: "HeroIllustration"
  key_links:
    - from: "src/components/story-form.tsx"
      to: "src/components/hero-illustration.tsx"
      via: "import and render above h1"
      pattern: "import.*HeroIllustration"
---

<objective>
Create the hero moon/stars illustration component and update the form page with solid button styling and refined vertical spacing.

Purpose: Adds visual warmth to the form page hero area (D-01, D-02), refines layout spacing (D-03, D-04), and modernizes the Generate button from gradient to solid teal (D-05, D-06).
Output: New hero-illustration.tsx component, updated story-form.tsx with all form-page polish changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/DESIGN.md
@.planning/phases/11-ui-polish-and-tidy-up/11-CONTEXT.md
@.planning/phases/11-ui-polish-and-tidy-up/11-UI-SPEC.md

@src/components/story-form.tsx
@src/components/loading-overlay.tsx
@src/app/globals.css

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/app/globals.css (color tokens used in this plan):
  --color-primary: #0c6681          (solid button fill)
  --color-primary-container: #9ae1ff (hero moon gradient start, hero star dots)
  --color-secondary-container: #fcd400 (hero gold star dots)
  --color-surface: #fbf9f1          (page background)
  --color-on-surface: #1b1c17       (text)

Spacing tokens:
  --spacing-sm: 8px
  --spacing-md: 16px
  --spacing-lg: 24px
  --spacing-xl: 32px
  --spacing-2xl: 48px
  --spacing-3xl: 64px

From src/components/loading-overlay.tsx (visual reference for hero illustration):
  Moon: w-16 h-16 rounded-full bg-gradient-to-br from-primary-container to-primary
  Stars: w-2 h-2 rounded-full bg-primary-container with twinkle animation
  Container: relative w-32 h-32
  @keyframes breathe: scale(1) <-> scale(1.08), opacity 0.8 <-> 1, 3s cycle

From src/components/story-form.tsx (current structure):
  Container: mx-auto max-w-[480px] px-lg pt-3xl pb-3xl
  Button: bg-gradient-to-br from-primary to-primary-container (line 89)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create hero illustration component and integrate into form</name>
  <files>src/components/hero-illustration.tsx, src/components/story-form.tsx</files>
  <read_first>
    - src/components/loading-overlay.tsx (reference for moon/stars visual pattern and inline style pattern)
    - src/components/story-form.tsx (current form structure, insertion point above h1)
    - src/app/globals.css (available color tokens)
    - .planning/phases/11-ui-polish-and-tidy-up/11-UI-SPEC.md (component spec section 1)
  </read_first>
  <action>
**Create `src/components/hero-illustration.tsx`** (per D-01, D-02):

```tsx
'use client'

export function HeroIllustration() {
  return (
    <div className="relative w-24 h-24 mx-auto mb-lg">
      <style>{`
        @keyframes gentleBreathe {
          0%, 100% { transform: scale(1); opacity: 0.85; }
          50% { transform: scale(1.05); opacity: 1; }
        }
      `}</style>

      {/* Moon - gradient circle echoing loading overlay */}
      <div
        className="absolute inset-0 m-auto w-12 h-12 rounded-full bg-gradient-to-br from-primary-container to-primary"
        style={{ animation: 'gentleBreathe 4s ease-in-out infinite' }}
      />
      {/* Gold stars */}
      <div
        className="absolute w-1.5 h-1.5 rounded-full bg-secondary-container"
        style={{ top: '8px', right: '16px', opacity: 0.7 }}
      />
      <div
        className="absolute w-1.5 h-1.5 rounded-full bg-secondary-container"
        style={{ bottom: '12px', right: '12px', opacity: 0.6 }}
      />
      {/* Light blue stars */}
      <div
        className="absolute w-1 h-1 rounded-full bg-primary-container"
        style={{ top: '16px', left: '10px', opacity: 0.5 }}
      />
      <div
        className="absolute w-1 h-1 rounded-full bg-primary-container"
        style={{ bottom: '20px', left: '18px', opacity: 0.4 }}
      />
    </div>
  )
}
```

Key details:
- Container is `w-24 h-24` (96x96px) -- compact, won't push content below fold on mobile
- Moon is `w-12 h-12` (48px) with same gradient as loading overlay (`from-primary-container to-primary`)
- Animation is `gentleBreathe` at 4s cycle (slower than loading overlay's 3s -- calmer for form page)
- Stars are static (no twinkle) -- form page should not be distracting
- 2 gold stars (`bg-secondary-container`) and 2 light blue stars (`bg-primary-container`)
- Uses inline `<style>` tag per established Tailwind v4 pattern
- `mb-lg` (24px) bottom margin before the h1 title
- `'use client'` directive (established pattern for all components)

**Update `src/components/story-form.tsx`** (per D-01, D-02, D-03, D-04, D-05, D-06):

1. Add import: `import { HeroIllustration } from './hero-illustration'`
2. Inside the `<div className="mx-auto max-w-[480px] ...">`, insert `<HeroIllustration />` as the first child, above the `<h1>` element
3. Change container top padding from `pt-3xl` to `pt-2xl` (64px -> 48px) to compensate for illustration height (per D-04 and UI-SPEC spacing adjustments)
4. Change button className from `bg-gradient-to-br from-primary to-primary-container` to `bg-primary` (per D-05)
5. Keep all other button classes unchanged: `mt-xl w-full h-[52px] rounded-full font-sans text-[0.75rem] font-semibold uppercase tracking-[0.04em] text-white transition-all duration-300 ease-in-out hover:brightness-110 disabled:opacity-40 disabled:cursor-not-allowed` (per D-06, hover:brightness-110 works on solid color)
6. Keep all other spacing values unchanged: `mt-sm` (tagline), `mt-2xl` (fields, themes), `mt-xl` (button)

The final button className should be exactly:
`mt-xl w-full h-[52px] rounded-full bg-primary font-sans text-[0.75rem] font-semibold uppercase tracking-[0.04em] text-white transition-all duration-300 ease-in-out hover:brightness-110 disabled:opacity-40 disabled:cursor-not-allowed`
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/components/hero-illustration.tsx exists and exports `HeroIllustration`
    - hero-illustration.tsx contains `@keyframes gentleBreathe`
    - hero-illustration.tsx contains `bg-gradient-to-br from-primary-container to-primary` (moon)
    - hero-illustration.tsx contains `bg-secondary-container` (gold stars)
    - hero-illustration.tsx contains `4s ease-in-out infinite` (slower animation)
    - hero-illustration.tsx contains `'use client'`
    - story-form.tsx contains `import { HeroIllustration } from './hero-illustration'`
    - story-form.tsx contains `<HeroIllustration` before the h1 element
    - story-form.tsx contains `pt-2xl` (not `pt-3xl`)
    - story-form.tsx contains `bg-primary` on the button (not `bg-gradient-to-br`)
    - story-form.tsx does NOT contain `from-primary to-primary-container` on the button
    - story-form.tsx still contains `hover:brightness-110` on the button
    - Full test suite passes (npx vitest run exits 0)
  </acceptance_criteria>
  <done>Hero illustration component renders above the title with breathing moon and static stars. Generate button is solid flat teal. Form top padding reduced from 64px to 48px. All existing tests still pass.</done>
</task>

</tasks>

<verification>
- `npx vitest run` -- full test suite passes (no regressions)
- `grep -r "HeroIllustration" src/` confirms import and usage
- `grep "bg-primary" src/components/story-form.tsx` confirms solid button (not gradient)
- `grep "pt-2xl" src/components/story-form.tsx` confirms reduced top padding
- `grep "gentleBreathe" src/components/hero-illustration.tsx` confirms animation
</verification>

<success_criteria>
- Form page displays a moon + stars illustration above the "Nightlight Tales" heading
- The illustration uses the same gradient and shape language as the loading overlay moon
- Generate button is solid teal, not gradient
- Button hover still works (brightness-110 on solid teal)
- Form spacing is tightened with hero illustration compensating for removed top padding
- All existing tests pass without modification
</success_criteria>

<output>
After completion, create `.planning/phases/11-ui-polish-and-tidy-up/11-01-SUMMARY.md`
</output>
