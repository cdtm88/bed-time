---
phase: 11-ui-polish-and-tidy-up
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/theme-grid.tsx
  - src/components/loading-overlay.tsx
autonomous: false

requirements:
  - D-07
  - D-08

must_haves:
  truths:
    - "Selected theme tile has a visible gold ring and light gold background tint"
    - "Loading overlay uses dark navy background with light-colored text"
    - "Loading overlay text is legible on the dark background"
    - "Moon and star animation colors are unchanged on loading overlay"
  artifacts:
    - path: "src/components/theme-grid.tsx"
      provides: "Gold-accented selected state for theme tiles"
      contains: "ring-secondary-container"
    - path: "src/components/loading-overlay.tsx"
      provides: "Dark navy loading overlay with reading-mode text colors"
      contains: "bg-reading-surface"
  key_links:
    - from: "src/components/loading-overlay.tsx"
      to: "src/app/globals.css"
      via: "reading-mode color tokens"
      pattern: "reading-on-surface"
    - from: "src/components/theme-grid.tsx"
      to: "src/app/globals.css"
      via: "secondary-container gold token"
      pattern: "secondary-container"
---

<objective>
Update theme tile selected state to gold accent and switch loading overlay to dark navy background.

Purpose: Unifies the gold accent role across the app (D-07) and creates a seamless atmospheric transition from form to loading to reading (D-08).
Output: Updated theme-grid.tsx with gold selection, updated loading-overlay.tsx with dark mode colors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/DESIGN.md
@.planning/phases/11-ui-polish-and-tidy-up/11-CONTEXT.md
@.planning/phases/11-ui-polish-and-tidy-up/11-UI-SPEC.md

@src/components/theme-grid.tsx
@src/components/loading-overlay.tsx
@src/app/globals.css

<interfaces>
<!-- Color tokens from globals.css used in this plan -->

Theme tile gold selection (D-07):
  --color-secondary-container: #fcd400  (gold ring + background tint)
  Current selected: bg-primary-container/15 ring-2 ring-primary/30
  Target selected: bg-secondary-container/10 ring-2 ring-secondary-container

Loading overlay dark mode (D-08):
  --color-reading-surface: #1a1a2e        (dark navy background)
  --color-reading-on-surface: #e8e0d0     (light text for heading)
  --color-reading-on-surface-muted: #a89f8f (muted text for subtext)
  Current background: bg-surface (#fbf9f1)
  Current text: text-on-surface (#1b1c17)
  Target background: bg-reading-surface
  Target heading text: text-reading-on-surface
  Target subtext: text-reading-on-surface-muted

Moon/stars colors (UNCHANGED):
  Moon: bg-gradient-to-br from-primary-container to-primary (#9ae1ff -> #0c6681)
  Stars: bg-primary-container (#9ae1ff)
  These light colors read well on dark navy -- no changes needed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update theme tile selected state to gold accent and loading overlay to dark mode</name>
  <files>src/components/theme-grid.tsx, src/components/loading-overlay.tsx</files>
  <read_first>
    - src/components/theme-grid.tsx (current selected state classes on lines 31-33)
    - src/components/loading-overlay.tsx (current bg-surface and text-on-surface classes)
    - src/app/globals.css (verify reading-mode tokens exist)
    - .planning/phases/11-ui-polish-and-tidy-up/11-UI-SPEC.md (component specs sections 3 and 4)
  </read_first>
  <action>
**Update `src/components/theme-grid.tsx`** (per D-07):

On lines 31-33, change the selected state conditional from:
```tsx
value === theme
  ? 'bg-primary-container/15 ring-2 ring-primary/30'
  : '',
```

To:
```tsx
value === theme
  ? 'bg-secondary-container/10 ring-2 ring-secondary-container'
  : '',
```

This replaces the faint teal selection with:
- `ring-2 ring-secondary-container` -- 2px solid gold (#fcd400) ring, fully opaque
- `bg-secondary-container/10` -- 10% opacity gold background tint (light enough not to obscure SVG illustrations)

Leave all other theme-grid.tsx code unchanged.

**Update `src/components/loading-overlay.tsx`** (per D-08):

1. Line 10: Change `bg-surface` to `bg-reading-surface` on the outer div className
   - Full className becomes: `fixed inset-0 z-50 flex flex-col items-center justify-center bg-reading-surface`

2. Line 54: Change heading text color from `text-on-surface` to `text-reading-on-surface`
   - Full className becomes: `font-serif text-[1.75rem] text-reading-on-surface`

3. Line 57: Change subtext color from `text-on-surface` to `text-reading-on-surface-muted`
   - Full className becomes: `font-serif text-[1rem] leading-relaxed text-reading-on-surface-muted mt-md`

Do NOT change:
- Moon gradient classes (`bg-gradient-to-br from-primary-container to-primary`)
- Star classes (`bg-primary-container`)
- Animation keyframes (`breathe`, `twinkle`)
- Any animation timing or delays
- The `mb-xl` on the moon/stars container
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - theme-grid.tsx contains `bg-secondary-container/10 ring-2 ring-secondary-container` (gold selected state)
    - theme-grid.tsx does NOT contain `bg-primary-container/15 ring-2 ring-primary/30` (old teal selected state)
    - loading-overlay.tsx contains `bg-reading-surface` (dark navy background)
    - loading-overlay.tsx does NOT contain `bg-surface` as a standalone class on the outer div
    - loading-overlay.tsx contains `text-reading-on-surface` (light heading text)
    - loading-overlay.tsx contains `text-reading-on-surface-muted` (muted subtext)
    - loading-overlay.tsx does NOT contain `text-on-surface` (old dark text -- would be invisible on dark background)
    - loading-overlay.tsx still contains `bg-gradient-to-br from-primary-container to-primary` (moon unchanged)
    - loading-overlay.tsx still contains `bg-primary-container` (stars unchanged)
    - loading-overlay.tsx still contains `@keyframes breathe` and `@keyframes twinkle` (animations unchanged)
    - Full test suite passes (npx vitest run exits 0)
  </acceptance_criteria>
  <done>Theme tiles show gold ring and gold tint when selected. Loading overlay has dark navy background with legible light-colored text. Moon and star animations are unchanged. All existing tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of all Phase 11 changes</name>
  <files>none</files>
  <action>
Human visual verification checkpoint. No code changes -- user reviews the combined output of Plan 01 and Plan 02 in the browser.
  </action>
  <what-built>
All Phase 11 visual polish changes across the form page, theme grid, and loading overlay:
- Hero moon/stars illustration above the title (D-01, D-02)
- Reduced top padding on form (D-03, D-04)
- Solid teal Generate button (D-05, D-06)
- Gold-accented theme tile selection (D-07)
- Dark navy loading overlay with light text (D-08)
  </what-built>
  <how-to-verify>
1. Run `npm run dev` and open http://localhost:3000
2. **Hero illustration:** Verify a moon + stars illustration appears above "Nightlight Tales" title. Moon should have a slow breathing animation. Stars should be static.
3. **Form layout:** Verify the form is horizontally centered with balanced padding. Content should start near the top (not vertically centered).
4. **Generate button:** Scroll to the bottom. The button should be solid teal (no gradient). Hover to verify slight brightness increase.
5. **Theme selection:** Click a theme tile. Verify it shows a gold ring border and a subtle gold background tint. The SVG illustration should still be clearly visible through the tint.
6. **Loading overlay:** Enter a name (e.g., "Luna"), select a theme, click Generate Story. Verify the loading overlay has a dark navy background with the moon/stars animation. Heading and subtext should be light-colored and legible.
7. **Transition feel:** Notice the atmospheric shift from light cream form -> dark navy loading. This is intentional.
  </how-to-verify>
  <verify>
    <automated>npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <done>User has visually approved all 8 decisions (D-01 through D-08) are correctly implemented.</done>
  <resume-signal>Type "approved" or describe any visual issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npx vitest run` -- full test suite passes (no regressions)
- `grep "ring-secondary-container" src/components/theme-grid.tsx` confirms gold ring
- `grep "bg-reading-surface" src/components/loading-overlay.tsx` confirms dark background
- `grep "text-reading-on-surface" src/components/loading-overlay.tsx` confirms light text
- Visual checkpoint confirms all 8 decisions are implemented correctly
</verification>

<success_criteria>
- Selected theme tiles display a visible gold ring and light gold tint
- Loading overlay background is dark navy (#1a1a2e), not light cream
- Loading overlay heading text is legible (#e8e0d0 on #1a1a2e)
- Loading overlay subtext is legible (#a89f8f on #1a1a2e)
- Moon/star animations on loading overlay are unchanged
- Visual checkpoint approved by user
- All existing tests pass without modification
</success_criteria>

<output>
After completion, create `.planning/phases/11-ui-polish-and-tidy-up/11-02-SUMMARY.md`
</output>
