---
phase: 08-theme-svg-assets
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/__tests__/theme-svg-assets.test.ts
  - public/themes/animals.svg
  - public/themes/space-stars.svg
  - public/themes/ocean-sea.svg
  - public/themes/fairy-tales.svg
  - public/themes/dragons.svg
  - public/themes/knights-castles.svg
  - public/themes/trains-vehicles.svg
  - public/themes/superheroes.svg
  - public/themes/robots.svg
  - public/themes/forest-nature.svg
  - public/themes/pirates.svg
  - public/themes/magic-school.svg
  - public/themes/farm-life.svg
  - public/themes/rainforest.svg
  - public/themes/underwater-adventure.svg
  - public/themes/dreams-clouds.svg
  - public/themes/holidays-seasons.svg
autonomous: true
requirements:
  - STORY-03

must_haves:
  truths:
    - "All 18 theme tiles have corresponding SVG files in public/themes/"
    - "Every SVG uses only the 4 permitted palette colors"
    - "Every SVG has viewBox 0 0 200 200 and is under 5KB"
    - "No SVG contains prohibited elements (image, text, style, script)"
  artifacts:
    - path: "src/__tests__/theme-svg-assets.test.ts"
      provides: "Automated validation of SVG structural constraints"
      min_lines: 40
    - path: "public/themes/animals.svg"
      provides: "Animals theme illustration"
      contains: "viewBox=\"0 0 200 200\""
    - path: "public/themes/space-stars.svg"
      provides: "Space & Stars theme illustration"
      contains: "viewBox=\"0 0 200 200\""
    - path: "public/themes/ocean-sea.svg"
      provides: "Ocean & Sea theme illustration"
      contains: "viewBox=\"0 0 200 200\""
    - path: "public/themes/fairy-tales.svg"
      provides: "Fairy Tales theme illustration"
      contains: "viewBox=\"0 0 200 200\""
    - path: "public/themes/dragons.svg"
      provides: "Dragons theme illustration"
      contains: "viewBox=\"0 0 200 200\""
    - path: "public/themes/knights-castles.svg"
      provides: "Knights & Castles theme illustration"
      contains: "viewBox=\"0 0 200 200\""
    - path: "public/themes/trains-vehicles.svg"
      provides: "Trains & Vehicles theme illustration"
      contains: "viewBox=\"0 0 200 200\""
    - path: "public/themes/superheroes.svg"
      provides: "Superheroes theme illustration"
      contains: "viewBox=\"0 0 200 200\""
    - path: "public/themes/robots.svg"
      provides: "Robots theme illustration"
      contains: "viewBox=\"0 0 200 200\""
    - path: "public/themes/forest-nature.svg"
      provides: "Forest & Nature theme illustration"
      contains: "viewBox=\"0 0 200 200\""
    - path: "public/themes/pirates.svg"
      provides: "Pirates theme illustration"
      contains: "viewBox=\"0 0 200 200\""
    - path: "public/themes/magic-school.svg"
      provides: "Magic School theme illustration"
      contains: "viewBox=\"0 0 200 200\""
    - path: "public/themes/farm-life.svg"
      provides: "Farm Life theme illustration"
      contains: "viewBox=\"0 0 200 200\""
    - path: "public/themes/rainforest.svg"
      provides: "Rainforest theme illustration"
      contains: "viewBox=\"0 0 200 200\""
    - path: "public/themes/underwater-adventure.svg"
      provides: "Underwater Adventure theme illustration"
      contains: "viewBox=\"0 0 200 200\""
    - path: "public/themes/dreams-clouds.svg"
      provides: "Dreams & Clouds theme illustration"
      contains: "viewBox=\"0 0 200 200\""
    - path: "public/themes/holidays-seasons.svg"
      provides: "Holidays & Seasons theme illustration"
      contains: "viewBox=\"0 0 200 200\""
  key_links:
    - from: "src/__tests__/theme-svg-assets.test.ts"
      to: "public/themes/*.svg"
      via: "fs.readFileSync reading each SVG"
      pattern: "readFileSync.*public/themes"
---

<objective>
Create all 17 missing SVG theme tile illustrations and a validation test suite that enforces structural constraints (viewBox, palette, file size, prohibited elements).

Purpose: Closes STORY-03 by providing visual assets for the theme selection grid. The integration layer (theme-grid.tsx, themeToFilename, Next.js Image) is already fully wired — these files are immediately served as static assets.

Output: 17 SVG files in public/themes/ + 1 test file validating all SVG constraints.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/DESIGN.md
@.planning/phases/08-theme-svg-assets/08-CONTEXT.md
@.planning/phases/08-theme-svg-assets/08-RESEARCH.md
@.planning/phases/08-theme-svg-assets/08-UI-SPEC.md
@.planning/phases/08-theme-svg-assets/08-VALIDATION.md
@src/lib/schemas.ts
@src/lib/theme-utils.ts

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/lib/schemas.ts:
```typescript
export const VALID_THEMES = [
  "Animals", "Dinosaurs", "Space & Stars", "Ocean & Sea",
  "Fairy Tales", "Dragons", "Knights & Castles", "Trains & Vehicles",
  "Superheroes", "Robots", "Forest & Nature", "Pirates",
  "Magic School", "Farm Life", "Rainforest", "Underwater Adventure",
  "Dreams & Clouds", "Holidays & Seasons",
] as const
```

From src/lib/theme-utils.ts:
```typescript
export function themeToFilename(theme: string): string {
  return theme
    .toLowerCase()
    .replace(/\s*&\s*/g, '-')
    .replace(/\s+/g, '-')
}
```

Filename mapping (themeToFilename output for each theme):
- Animals -> animals
- Dinosaurs -> dinosaurs (ALREADY EXISTS)
- Space & Stars -> space-stars
- Ocean & Sea -> ocean-sea
- Fairy Tales -> fairy-tales
- Dragons -> dragons
- Knights & Castles -> knights-castles
- Trains & Vehicles -> trains-vehicles
- Superheroes -> superheroes
- Robots -> robots
- Forest & Nature -> forest-nature
- Pirates -> pirates
- Magic School -> magic-school
- Farm Life -> farm-life
- Rainforest -> rainforest
- Underwater Adventure -> underwater-adventure
- Dreams & Clouds -> dreams-clouds
- Holidays & Seasons -> holidays-seasons
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SVG validation test suite (Wave 0)</name>
  <files>src/__tests__/theme-svg-assets.test.ts</files>
  <read_first>
    - src/lib/schemas.ts (VALID_THEMES array — source of truth for theme names)
    - src/lib/theme-utils.ts (themeToFilename function — derives filenames)
    - public/themes/dinosaurs.svg (existing SVG — verify integration path)
    - vitest.config.ts (test config)
  </read_first>
  <action>
Create `src/__tests__/theme-svg-assets.test.ts` that validates all 18 SVG files (17 new + 1 existing dinosaurs.svg) against structural constraints. Use `fs.readFileSync` to read each file from `path.join(process.cwd(), 'public/themes/', filename + '.svg')`.

Import VALID_THEMES from `@/lib/schemas` and themeToFilename from `@/lib/theme-utils`.

Tests to write:

1. **"all 18 theme SVGs exist"** — Loop VALID_THEMES, assert each `public/themes/{themeToFilename(theme)}.svg` file exists using `fs.existsSync`.

2. **"each SVG has viewBox 0 0 200 200"** — For each SVG file, read contents and assert it contains `viewBox="0 0 200 200"`.

3. **"each SVG uses only permitted palette colors"** — For each SVG file (excluding dinosaurs.svg which is legacy), extract all `fill="..."` and `stroke="..."` attribute values using regex `/(?:fill|stroke)="(#[0-9a-fA-F]{6})"/g`. Assert every extracted color is one of: `#0c6681`, `#705d00`, `#fbf9f1`, `#1b1c17`. Allow `fill="none"` and `stroke="none"`.

4. **"each new SVG is under 5KB"** — For each SVG file (excluding dinosaurs.svg), assert `fs.statSync(filepath).size < 5120`.

5. **"no SVG contains prohibited elements"** — For each SVG file (excluding dinosaurs.svg), assert the content does NOT match any of: `/<image[\s>]/`, `/<text[\s>]/`, `/<style[\s>]/`, `/<script[\s>]/`.

Use `describe('Theme SVG Assets')` with `it.each` or a loop over VALID_THEMES for parameterized tests. Skip dinosaurs.svg from palette/size/prohibited checks by filtering on filename.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/theme-svg-assets.test.ts --reporter=verbose 2>&1 | head -50</automated>
  </verify>
  <acceptance_criteria>
    - src/__tests__/theme-svg-assets.test.ts exists and contains `describe('Theme SVG Assets'`
    - File imports from `@/lib/schemas` and `@/lib/theme-utils`
    - File contains `fs.readFileSync` or `fs.existsSync`
    - File contains checks for `viewBox="0 0 200 200"`
    - File contains the 4 permitted hex values: `#0c6681`, `#705d00`, `#fbf9f1`, `#1b1c17`
    - File contains regex patterns for prohibited elements: `image`, `text`, `style`, `script`
    - File contains size check `5120` or `5 * 1024`
    - Test run shows failures for the 17 missing SVGs (expected — they don't exist yet)
  </acceptance_criteria>
  <done>Test file exists, runs without errors, and correctly reports failures for missing SVGs</done>
</task>

<task type="auto">
  <name>Task 2: Create all 17 SVG theme tile illustrations</name>
  <files>
    public/themes/animals.svg
    public/themes/space-stars.svg
    public/themes/ocean-sea.svg
    public/themes/fairy-tales.svg
    public/themes/dragons.svg
    public/themes/knights-castles.svg
    public/themes/trains-vehicles.svg
    public/themes/superheroes.svg
    public/themes/robots.svg
    public/themes/forest-nature.svg
    public/themes/pirates.svg
    public/themes/magic-school.svg
    public/themes/farm-life.svg
    public/themes/rainforest.svg
    public/themes/underwater-adventure.svg
    public/themes/dreams-clouds.svg
    public/themes/holidays-seasons.svg
  </files>
  <read_first>
    - .planning/phases/08-theme-svg-assets/08-UI-SPEC.md (SVG specification, visual concepts per theme, color palette)
    - .planning/phases/08-theme-svg-assets/08-CONTEXT.md (D-01 through D-04 locked decisions)
    - .planning/DESIGN.md (color tokens, no-sharp-corners rule)
    - src/__tests__/theme-svg-assets.test.ts (validation constraints to satisfy)
  </read_first>
  <action>
Create 17 SVG files in `public/themes/`. Per D-01, D-02, D-03, D-04:

**Every SVG must follow this template structure:**
```xml
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 200 200" fill="none">
  <rect width="200" height="200" rx="24" fill="#fbf9f1"/>
  <!-- 3-5 themed shapes -->
</svg>
```

**Constraints (per D-04):**
- viewBox="0 0 200 200" (exact)
- Only 4 colors for fill/stroke: #0c6681 (primary/teal), #705d00 (gold), #fbf9f1 (cream/light), #1b1c17 (dark ink)
- All strokes: stroke-width="2" or "3", stroke-linecap="round", stroke-linejoin="round"
- All rectangles must have rx attribute (no sharp corners)
- No `<image>`, `<text>`, `<style>`, `<script>` elements
- No gradients, no filters, no external references
- Target under 5KB per file (3-5 simple shapes keeps this easy)

**Visual concepts per theme (from UI-SPEC):**

1. **animals.svg** — Sitting cat silhouette, paw print, small bird
2. **space-stars.svg** — Crescent moon, two stars, Saturn-like ringed planet
3. **ocean-sea.svg** — Wave curves, fish, shell
4. **fairy-tales.svg** — Crown, magic wand with star tip, open book
5. **dragons.svg** — Dragon silhouette (wing + tail), flame wisp, mountain peak
6. **knights-castles.svg** — Castle tower with flag, shield, sword
7. **trains-vehicles.svg** — Train engine front, wheel, smoke puff
8. **superheroes.svg** — Cape silhouette, star burst, mask
9. **robots.svg** — Boxy robot body, antenna, gear
10. **forest-nature.svg** — Pine tree, leaf, mushroom
11. **pirates.svg** — Pirate hat, treasure chest, anchor
12. **magic-school.svg** — Wizard hat, potion bottle, spell sparkle
13. **farm-life.svg** — Barn silhouette, sunflower, fence section
14. **rainforest.svg** — Tropical leaf, toucan, vine
15. **underwater-adventure.svg** — Submarine, bubbles, seaweed
16. **dreams-clouds.svg** — Fluffy cloud, sleeping moon face, stars
17. **holidays-seasons.svg** — Snowflake, sun, leaf (representing multiple seasons)

**Style (per D-02):** Simple, clean, iconic storybook. 3-5 recognizable shapes. Lightweight vector paths using basic SVG elements (circle, rect, ellipse, path, polygon, line, polyline, g). The shapes should be immediately recognizable as their theme at 200x200 size.

After creating all 17 files, run the validation test suite to confirm all constraints pass.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/theme-svg-assets.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - All 17 SVG files exist in public/themes/
    - Each file contains `viewBox="0 0 200 200"`
    - Each file contains only the 4 permitted hex colors (#0c6681, #705d00, #fbf9f1, #1b1c17) plus fill="none"
    - Each file is under 5KB (5120 bytes)
    - No file contains `<image`, `<text`, `<style`, or `<script` elements
    - Each file contains `xmlns="http://www.w3.org/2000/svg"`
    - Each file starts with `<svg`
    - npx vitest run src/__tests__/theme-svg-assets.test.ts exits 0 with all tests passing
  </acceptance_criteria>
  <done>All 17 SVG files created and passing all automated validation tests</done>
</task>

</tasks>

<verification>
npx vitest run src/__tests__/theme-svg-assets.test.ts --reporter=verbose
All tests pass: file existence, viewBox, palette colors, file size, prohibited elements.
</verification>

<success_criteria>
- 17 new SVG files exist in public/themes/ alongside existing dinosaurs.svg (18 total)
- All SVG validation tests pass (existence, viewBox, palette, size, prohibited elements)
- Each SVG is a clean, iconic storybook illustration with 3-5 recognizable shapes
</success_criteria>

<output>
After completion, create `.planning/phases/08-theme-svg-assets/08-01-SUMMARY.md`
</output>
