---
phase: 07-nightlight-tales-branding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - README.md
  - src/app/layout.tsx
  - src/app/icon.tsx
autonomous: false
requirements: []
must_haves:
  truths:
    - "package.json name field is 'nightlight-tales', not 'bed-time'"
    - "README.md opens with '# Nightlight Tales' and includes features plus setup instructions"
    - "Shared links display 'Nightlight Tales' title and description via Open Graph tags"
    - "Browser tab shows a moon emoji favicon instead of the default Next.js icon"
    - "All existing tests still pass (no regressions from branding changes)"
  artifacts:
    - path: "package.json"
      provides: "Updated package name"
      contains: "nightlight-tales"
    - path: "README.md"
      provides: "Project documentation"
      contains: "# Nightlight Tales"
    - path: "src/app/layout.tsx"
      provides: "Open Graph metadata"
      contains: "openGraph"
    - path: "src/app/icon.tsx"
      provides: "Moon emoji favicon"
      contains: "ImageResponse"
  key_links:
    - from: "src/app/icon.tsx"
      to: "browser favicon"
      via: "Next.js App Router file convention"
      pattern: "export default function Icon"
    - from: "src/app/layout.tsx"
      to: "HTML meta tags"
      via: "Next.js Metadata export"
      pattern: "openGraph.*title.*Nightlight Tales"
---

<objective>
Rebrand all app identity references from "bed-time" to "Nightlight Tales" across package metadata, README, Open Graph tags, and favicon.

Purpose: Establish consistent "Nightlight Tales" branding so the app name is correct in package registries, browser tabs, shared links, and repository documentation.
Output: Updated package.json/lock, rewritten README.md, OG metadata in layout.tsx, moon emoji favicon via icon.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-nightlight-tales-branding/07-CONTEXT.md
@.planning/phases/07-nightlight-tales-branding/07-RESEARCH.md
@.planning/phases/07-nightlight-tales-branding/07-UI-SPEC.md

<interfaces>
<!-- Current layout.tsx metadata export (to be extended with openGraph) -->
From src/app/layout.tsx:
```typescript
export const metadata: Metadata = {
  title: "Nightlight Tales",
  description: "Personalized bedtime stories for children",
};
```

<!-- Current package.json (name field to change) -->
From package.json:
```json
{
  "name": "bed-time",
  "version": "0.1.0",
  "private": true
}
```

<!-- Current README.md (full rewrite needed) -->
From README.md:
```markdown
# bed-time
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename package and rewrite README</name>
  <files>package.json, package-lock.json, README.md</files>
  <read_first>
    - package.json (current name field and full structure)
    - README.md (current content)
    - .planning/phases/07-nightlight-tales-branding/07-RESEARCH.md (README template in Code Examples section)
  </read_first>
  <action>
**package.json** (per D-01): Change the `"name"` field from `"bed-time"` to `"nightlight-tales"`. Do NOT change any other field. The full first three lines should read:
```json
{
  "name": "nightlight-tales",
  "version": "0.1.0",
```

**package-lock.json**: After editing package.json, run `npm install` to regenerate the lock file. Do NOT manually edit package-lock.json. The `npm install` command will update the root `name` field in the lock file automatically.

**README.md** (per D-02): Replace the entire file with the following content:

```markdown
# Nightlight Tales

Personalized bedtime stories for children, powered by AI.

## Features

- Generate safe, age-appropriate bedtime stories personalized with your child's name
- Choose from 15+ themes like dinosaurs, space, ocean, and fairy tales
- Select reading duration (3, 5, 10, or 15 minutes)
- Age-adapted vocabulary and complexity for children 0-10
- Calming, wind-down language designed to ease children toward sleep
- Distraction-free reading mode optimized for dim bedrooms
- Post-generation safety validation on every story

## Getting Started

### Prerequisites

- Node.js 18+
- An Anthropic API key ([get one here](https://console.anthropic.com/))

### Setup

1. Clone the repository:
   ```bash
   git clone https://github.com/yourusername/nightlight-tales.git
   cd nightlight-tales
   ```
2. Install dependencies:
   ```bash
   npm install
   ```
3. Create a `.env.local` file with your API key:
   ```
   ANTHROPIC_API_KEY=your-key-here
   ```
4. Start the development server:
   ```bash
   npm run dev
   ```
5. Open [http://localhost:3000](http://localhost:3000)

## Tech Stack

- [Next.js](https://nextjs.org/) 16 (App Router, Edge Runtime)
- [React](https://react.dev/) 19
- [Tailwind CSS](https://tailwindcss.com/) v4
- [Claude API](https://docs.anthropic.com/) (Anthropic) for story generation and safety validation
```

IMPORTANT: Do NOT change "bedtime" in any file other than package.json and README.md. The word "bedtime" in src/lib/prompts.ts and test files is a descriptive English word, not the brand name.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(p.name!=='nightlight-tales') { console.error('package.json name is', p.name); process.exit(1); } console.log('OK: package.json name is nightlight-tales')" && head -1 README.md | grep -q "# Nightlight Tales" && echo "OK: README title correct" && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"name": "nightlight-tales"`
    - package-lock.json contains `"name": "nightlight-tales"` at the root level
    - README.md line 1 is `# Nightlight Tales`
    - README.md contains `## Features`
    - README.md contains `## Getting Started`
    - README.md contains `## Tech Stack`
    - README.md contains `ANTHROPIC_API_KEY`
    - `npx vitest run` exits 0 (no regressions)
  </acceptance_criteria>
  <done>package.json name is "nightlight-tales", lock file regenerated, README rewritten with title, features, setup instructions, and tech stack sections</done>
</task>

<task type="auto">
  <name>Task 2: Add Open Graph metadata and moon emoji favicon</name>
  <files>src/app/layout.tsx, src/app/icon.tsx</files>
  <read_first>
    - src/app/layout.tsx (current metadata export to extend)
    - .planning/phases/07-nightlight-tales-branding/07-RESEARCH.md (Pattern 1 and Pattern 2 code examples)
    - .planning/phases/07-nightlight-tales-branding/07-UI-SPEC.md (favicon specification and copywriting contract)
  </read_first>
  <action>
**src/app/layout.tsx** (per D-03): Extend the existing `metadata` export to include an `openGraph` property. The `title` and `description` top-level fields remain unchanged. Add the `openGraph` object with explicit `title`, `description`, and `type` values (OG does NOT inherit from top-level — values must be duplicated). The metadata export should become:

```typescript
export const metadata: Metadata = {
  title: "Nightlight Tales",
  description: "Personalized bedtime stories for children",
  openGraph: {
    title: "Nightlight Tales",
    description: "Personalized bedtime stories for children",
    type: "website",
  },
};
```

Do NOT change any other part of layout.tsx — the font imports, font variables, RootLayout function, body classes, and html attributes all remain exactly as they are.

**src/app/icon.tsx** (per D-04): Create a new file that uses the Next.js App Router programmatic icon generation pattern. The file exports a default function returning an `ImageResponse` with the moon emoji rendered as a 32x32 PNG. Exact file contents:

```typescript
import { ImageResponse } from "next/og";

export const size = { width: 32, height: 32 };
export const contentType = "image/png";

export default function Icon() {
  return new ImageResponse(
    (
      <div
        style={{
          fontSize: 24,
          width: "100%",
          height: "100%",
          display: "flex",
          alignItems: "center",
          justifyContent: "center",
          background: "transparent",
        }}
      >
        🌙
      </div>
    ),
    { ...size }
  );
}
```

Do NOT add any `<link rel="icon">` tags manually in layout.tsx — Next.js handles favicon injection automatically when `icon.tsx` exists in the app directory.
  </action>
  <verify>
    <automated>grep -q "openGraph" src/app/layout.tsx && echo "OK: openGraph present in layout.tsx" && grep -q 'title: "Nightlight Tales"' src/app/layout.tsx && echo "OK: OG title present" && test -f src/app/icon.tsx && echo "OK: icon.tsx exists" && grep -q "ImageResponse" src/app/icon.tsx && echo "OK: icon.tsx uses ImageResponse" && npx vitest run && npx next build</automated>
  </verify>
  <acceptance_criteria>
    - src/app/layout.tsx contains `openGraph: {`
    - src/app/layout.tsx openGraph block contains `title: "Nightlight Tales"`
    - src/app/layout.tsx openGraph block contains `description: "Personalized bedtime stories for children"`
    - src/app/layout.tsx openGraph block contains `type: "website"`
    - src/app/layout.tsx still contains `const notoSerif` and `const plusJakartaSans` (fonts untouched)
    - src/app/icon.tsx exists
    - src/app/icon.tsx contains `import { ImageResponse } from "next/og"`
    - src/app/icon.tsx contains `export const size = { width: 32, height: 32 }`
    - src/app/icon.tsx contains `export const contentType = "image/png"`
    - src/app/icon.tsx contains the moon emoji character
    - `npx vitest run` exits 0
    - `npx next build` exits 0
  </acceptance_criteria>
  <done>layout.tsx metadata includes openGraph with title, description, and type; icon.tsx generates 32x32 moon emoji PNG favicon; build succeeds with no regressions</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify branding in browser</name>
  <what-built>Complete Nightlight Tales branding: package name, README, Open Graph tags, and moon emoji favicon</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:3000
    2. Check the browser tab — it should show a moon emoji (🌙) favicon and "Nightlight Tales" as the page title
    3. Open browser DevTools > Elements tab, inspect `<head>` — confirm `<meta property="og:title" content="Nightlight Tales">` and `<meta property="og:description" content="Personalized bedtime stories for children">` are present
    4. Open README.md in the repo — confirm it has the full rewritten content with Features, Getting Started, and Tech Stack sections
    5. Check package.json name field shows "nightlight-tales"
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `node -e "const p=require('./package.json'); if(p.name!=='nightlight-tales') process.exit(1)"` — package name correct
2. `head -1 README.md | grep -q "# Nightlight Tales"` — README title correct
3. `grep -q "openGraph" src/app/layout.tsx` — OG metadata present
4. `test -f src/app/icon.tsx` — favicon file exists
5. `npx vitest run` — no test regressions
6. `npx next build` — build succeeds with new metadata and favicon
</verification>

<success_criteria>
- package.json and package-lock.json both use name "nightlight-tales"
- README.md is a complete project README with title, features, setup, and tech stack
- layout.tsx metadata includes openGraph with title, description, and type
- icon.tsx generates a 32x32 moon emoji PNG favicon
- All existing vitest tests pass
- Next.js build succeeds
- Visual confirmation: moon favicon visible in browser tab
</success_criteria>

<output>
After completion, create `.planning/phases/07-nightlight-tales-branding/07-01-SUMMARY.md`
</output>
